const query = gql`
  query {
    subjects {
      data {
        attributes {
          Slug
        }
      }
    }
  }
`;

export default function Layout({ children }) {
    return (
      
    );
  }